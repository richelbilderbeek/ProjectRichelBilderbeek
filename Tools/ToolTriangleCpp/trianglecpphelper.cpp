#include "trianglecpphelper.h"

#include <cstdio>
#include <boost/algorithm/string/split.hpp>

/*
int ribi::tricpp::DumbRand(const int max)
{
  return std::rand() % max;
  //static int s_randomseed = 1;
  //s_randomseed = (s_randomseed * 1366l + 150889l) % 714025l;
    //return s_randomseed / (714025l / choices + 1);
}

//From http://www.richelbilderbeek.nl/CppSeperateString.htm
std::vector<std::string> ribi::tricpp::SeperateString(
  const std::string& input,
  const char seperator)
{
  std::vector<std::string> v;
  boost::algorithm::split(v,input,
    std::bind2nd(std::equal_to<char>(),seperator),
    boost::algorithm::token_compress_on);
  return v;
}
*/
