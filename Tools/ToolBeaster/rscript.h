#ifndef RSCRIPT_H
#define RSCRIPT_H

/*
#include <string>

struct Rscript
{
  Rscript(const std::string& log_filename);
};

std::ostream& operator<<(std::ostream& os, const Rscript& script);
*/
#endif // RSCRIPT_H
