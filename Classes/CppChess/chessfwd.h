
#ifndef CHESSFWD_H
#define CHESSFWD_H

///Contains the CppChess forward declarations
namespace Chess
{
  struct BitBoard;
  struct Board;
  struct BoardFactory;
  struct BoardWidget;
  enum class Castling;
  enum class Color;
  struct File;
  struct Game;
  struct GameWidget;
  struct Move;
  struct MoveFactory;
  struct Piece;
  enum class Player;
  struct Rank;
  struct Score;
  struct Square;
  struct SquareSelector;
  struct Widget;
}

#endif // CHESSFWD_H
